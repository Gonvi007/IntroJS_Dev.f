// 1.- Hacer una función que convierta de grados centígrados a Farenheit

/*
var centigrados = parseInt( prompt ("Ingresa temperatura en Celcius"))

function grados (temperaturaC){
    var resultado = (temperaturaC * (9/5)) + 32;
    return (resultado + " F°");

}

var farenheit = grados(centigrados);

console.log(grados (centigrados) );

*/
// 2.- Hacer una función que muestre la tabla de multiplicar de un número

// var multiplicar = prompt("Ingrese un numero del 1 al 10");

// function tablaMultiplicar(number) {
//     var resultado = "";
//     var cont = 1;

//     for (let index = 0; index < 10; index++) {
//         resultado += parseInt(number) + " X " parseInt(cont) + " = " + (number * cont) + " /n ";
//         cont++
//     }
//     return resultado;
// }

// console.log(tablaMultiplicar(multi));

// 3.- Leer un arreglo de enteros y obtener su promedio

// var numeros = [1,2,3,4,5,6,7];
// var sumaTotal=0;
// var division=0;
// for(var index=0; index < numeros.length; index++) {
//     sumaTotal = sumaTotal + numeros[index];
// }
// division = sumaTotal / numeros.length;

// function promedio(numeros) {
//     var sumaTotal = 0;
//     var division = 0;

//     for (var index = 0; index < numeros.length; index++) {
//         sumaTotal = sumaTotal +  numeros[index];
//     }
//     division = sumaTotal / numeros.length;
//     return promedio;
// }

// console.log(promedio(numeros));



// 4.- Crear una funcion que reciba como parametro una oración y defina si es palindromo
// 5.- Escribe una función que halle l máximo entre tres números


// Crea una función que reciba 2 parámetros, precio e iva, y devuelva el precio con iva incluido. Si no recibe el iva, aplicará el 21 por ciento por defecto.
// Crea una función que reciba un texto y lo devuelva al revés


//  Escribe una función que imprima una
//    pirámide de astericos como la siguiente:
//                    *
//                   * *
//                  * * *
//                 * * * *
//                * * * * *
// ---------------------------------------------


// Ejercicios ---
// Realiza un script que pida por teclado 3 edades y 3 nombres e indique el nombre del mayor
// Realiza un script que genere un número aleatorio entre 1 y 99
// Realiza un script que pida un texto y lo muestre en mayúsculas.
// Realiza un juego de Piedra Papel Tijera contra el Pc
// Hacer una funcion que calcule el tiempo
//   necesario para que un automóvil que se
//   mueve con una velocidad de 73000 m/h
//   recorra una distancia de 120 km.
//             (TIEMPO = d/v) (editado) 

            